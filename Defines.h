#pragma once
#include <algorithm>
#include <string>
#include <sstream>
#include <vector>

using std::string;
using std::stringstream;
using std::vector;

enum caseSettings
{
	notUseCase,
	mixedCase,
	upperCase,
	lowerCase,
};
